# Power Platform fidelity and device UX

## Scope

Presentation and editorial finishing of the existing static portfolio. No new demos, dependencies, backend, production connections, or analytical fixtures. Primary implemented recreations remain distinct from architecture/concept/strategy prototypes.

- Added the supported Microsoft-surface stylesheet to all 15 interactive routes: SharePoint-style portal/pages, Canvas-style workflows, Power BI-style reports, and Forms-style intake.
- Preserved the operational/reporting distinction in EHS and Toolbox Talks. Report tabs and measured/estimated/modeled/missing/partial/upper-bound labels remain visible.
- Bounded phone/tablet previews with internal scrolling, keyboard focus, restrained frames and appropriate sticky headers. Full-workspace views remain full workspaces.
- Corrected CMMS light-control colors and a CSS override of a hidden empty-state panel.
- Standardized public Systems terminology without changing stable demos URLs. Footer navigation is Work, Systems, Smart Factory, About, Resume. Work links favor primary recreations.
- Removed three one-time implementation helpers. Unrelated social-thumbnail variants were left untouched.

## Browser validation

The prior four-width review (320, 375, 768, 1440 pixels) was retained, with targeted checks after device edits rather than repeating the entire sweep.

- Ladder: correct selected asset and completed inspection/history; bounded phone and tablet; wheel scrolling moved content without increasing the frame.
- Kanban: refill removed the open request; the same item subsequently created a valid new request; original closeout and new request remained in the audit trail. Back to request selects the requester phone. Both roles retain bounded previews.
- Facility leaks: location changes preserved form text; submitted synthetic report retained the selected coordinate; mobile/admin content stayed inside the bounded surface.
- CMMS: keyboard PageDown scrolled within the bounded preview; generated record remained intact; light fields and hidden empty state corrected.
- EHS: evidence gate and report refresh distinction verified; completion changed only in the refreshed reporting snapshot.
- Toolbox Talks: admin report route/refresh reviewed. Electrical: all five report tabs reviewed. Fleet: pilot SOC guardrail rejected the unsuitable setting.
- Forms: required fields blocked empty submission; completed synthetic submission produced a receipt.
- Connected Operations: scenario changed displayed OEE; Smart Factory input/report layout reviewed. Portal links and calendar reviewed.
- Targeted narrow device checks found no document-level horizontal overflow. Console review returned no warnings/errors.

## Release gates

### Visual de-identification review

Compared private platform/signage references with the public source and rendered screens. Replaced the teal/green action-tile and cyan-header combination with an independently selected public palette: Fluent-style blue #0f6cbd, slate #243a5e, neutral gray/white, and violet #8764b8 for secondary analytical series. Green remains a conventional success indication. SharePoint links are neutral outlined tiles rather than source-style solid green tiles. Canvas controls and report themes follow this independent palette; Forms remains a neutral questionnaire.

No source logos, corporate motifs, account identities, photographs, screenshots, tenant URLs or unique employer identifiers are used in the recreated interfaces. The leak drawing is a small invented L-shaped schematic with generic rooms, not the source plan. The public Kanban sign is a single-item synthetic illustration with a vertical instruction sequence, not the source multi-item/central-QR composition. Operational terms remain generic. This review reduces visual identification risk; it cannot guarantee that someone familiar with the work will not recognize the process itself.

Run the site validator, system-data tests, diff whitespace check, and tracked-source privacy scan before staging. Private reference material must remain ignored and untracked. Stage only intentional release files, excluding unrelated thumbnails. Verify GitHub validation, Pages deployment, and live asset versions after push; this document alone is not deployment evidence.

## Limits and deliberately unchanged behavior

These are synthetic browser recreations, not embedded Microsoft applications or proof of tenant deployment. No real plant drawings, employer screenshots, account chrome, credentials or live connections are included. Native touch scrolling is provided by CSS; a physical phone/tablet touch test was not performed. This is not a full assistive-technology audit or a pixel-perfect clone. Existing transactional rules, interval calculations, modeled boundaries and synthetic records were preserved. Kanban uses physical reorder signals, not a newly invented live remaining-inventory count.
