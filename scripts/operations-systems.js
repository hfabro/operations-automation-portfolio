(function(){'use strict';
 const root=document.querySelector('#system');if(!root)return;
 const kind=document.body.dataset.system,data=window.OPS_DATA;
 const esc=v=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const number=(v,d=0)=>Number(v).toLocaleString('en-US',{maximumFractionDigits:d});
 const message=text=>{document.querySelector('#ops-live').textContent=text;};
 const heading=(title,subtitle)=>`<div class="ops-toolbar"><div><strong>${title}</strong><small>${subtitle}</small></div><a href="../plant-operations-hub/">← Plant hub</a></div>`;
 const kpis=items=>`<div class="ops-kpis">${items.map(([label,value])=>`<div class="ops-kpi"><span>${label}</span><strong>${value}</strong></div>`).join('')}</div>`;
 const table=(caption,heads,rows)=>`<div class="ops-table-wrap" tabindex="0" role="region" aria-label="${caption}"><table><caption>${caption}</caption><thead><tr>${heads.map(h=>`<th scope="col">${h}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${row.map(c=>`<td>${c}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;
 const bars=(title,rows,max=100,unit='%')=>`<article class="ops-card"><h3>${title}</h3><div class="ops-bars">${rows.map(([name,value])=>`<div class="ops-bar-row"><span>${name}</span><div class="ops-bar-track"><div class="ops-bar-fill green" style="width:${Math.max(0,Math.min(100,value/max*100))}%"></div></div><strong>${number(value,1)}${unit}</strong></div>`).join('')}</div></article>`;
 const line=(title,values,unit,secondary)=>{const max=Math.max(1,...values,...(secondary||[]))*1.1;const points=arr=>arr.map((v,i)=>`${45+i*635/Math.max(1,arr.length-1)},${210-v/max*175}`).join(' ');return `<article class="ops-card"><h3>${title}</h3><svg class="ops-chart" viewBox="0 0 710 245" role="img" aria-label="${title}; ${unit}. Accessible values in the detail table."><path class="gridline" d="M45 35H680M45 122H680M45 210H680"/><text x="2" y="35">${number(max)}</text><text x="25" y="213">0</text><text x="45" y="237">First interval</text><text x="580" y="237">Last interval</text><polyline points="${points(values)}"/>${secondary?`<polyline class="secondary" points="${points(secondary)}"/>`:''}</svg><p class="ops-caption">${unit}${secondary?' · Blue: site load / amber: charging model':''}</p></article>`;};
 const tabs=(labels,active)=>`<nav class="ops-tabs" aria-label="System views">${labels.map((label,i)=>`<button type="button" data-tab="${i}" aria-pressed="${active===i}">${label}</button>`).join('')}</nav>`;
 function wireTabs(callback){root.querySelectorAll('[data-tab]').forEach(b=>b.addEventListener('click',()=>callback(+b.dataset.tab)));}
 const option=(text,value=text)=>`<option value="${value}">${text}</option>`;
 const select=(label,id,values)=>`<label>${label}<select id="${id}">${values.map(v=>Array.isArray(v)?option(v[0],v[1]):option(v)).join('')}</select></label>`;
 const action=(label,id,primary=false)=>`<button type="button" id="${id}" class="${primary?'primary':''}">${label}</button>`;
 function focusTitle(){const h=root.querySelector('h2,h3');if(h){h.tabIndex=-1;h.focus({preventScroll:true});}}
 function calendar(){const events={2:'Contractor: air-service visit',7:'Routine grounds service',10:'Building envelope review',15:'Monthly supplies review',18:'Preventive service · day 1',19:'Preventive service · day 2',24:'Evidence review',28:'Recurring service visit'};return `<h3>Facilities calendar · September 2026</h3><p class="ops-caption">Fictional planned work. Each event has an accountable coordinator; a calendar is not the execution record.</p><div class="ops-calendar">${Array.from({length:30},(_,i)=>`<div class="ops-day"><strong>Sep ${i+1}</strong>${events[i+1]?`<span>${events[i+1]}</span>`:''}</div>`).join('')}</div>`;}
 function hub(){const card=(icon,title,sub,slug)=>`<a class="portal-link" href="${slug.startsWith('#')?slug:'../'+slug.replace('#','/#')+(slug.includes('#')?'':'/')}"><span class="portal-icon" aria-hidden="true">${icon}</span><strong>${title}</strong><small>${sub} →</small></a>`;
 root.innerHTML=heading('Plant Operations Hub','Manufacturing Campus · Synthetic recreation')+`<section class="ops-band"><h2>One place to start the work.</h2><p>Report a condition, complete an inspection, request supplies, or review operational performance.</p><p>Herramientas para reportar, inspeccionar y dar seguimiento.</p></section><div class="ops-pad"><h2>Employee tools</h2><div class="ops-cards">${card('⌖','Report a facility leak','Reportar una fuga','facility-leaks')}${card('▤','Toolbox talks','Materiales y confirmación','toolbox-talks')}${card('☑','Ladder inspection','Inspección de escaleras','ladder-inspection')}${card('＋','Service issue','Reportar un problema de servicio','service-intake')}</div><h2 class="ops-section-title">Facilities operations</h2><div class="ops-cards">${card('⌖','Open leak reports','Assign, respond, verify','facility-leaks')}${card('▤','Toolbox talks admin','Completion records and reporting','toolbox-talks#admin')}${card('▦','Facilities calendar','Planned visits and recurring work','#facilities-calendar')}${card('▦','EHS compliance control','Requirements, tasks, evidence','ehs-control')}${card('▣','Supply replenishment','Physical signal to confirmed refill','digital-kanban')}</div><h2 class="ops-section-title">Dashboard & analytics</h2><div class="ops-cards">${card('↗','Electrical usage','Demand, context, billing evidence','electrical-analytics')}${card('▰','Forklift fleet & battery','Readiness and charging decisions','forklift-fleet')}${card('▦','Compliance management','Completion, evidence and exceptions','ehs-control#report')}</div><details id="facilities-calendar" class="ops-card ops-section-title" open><summary>Facilities calendar</summary><div>${calendar()}</div></details><section class="ops-card ops-section-title"><h2>Manufacturing Operations Digital Platform</h2><ol class="ops-flow"><li>Physical work</li><li>SharePoint hub</li><li>Forms / Power Apps</li><li>Lists records</li><li>Automate / API</li><li>Power BI</li><li>Owned decisions</li></ol><p>The portal is the front door. Operational records remain the source of truth; reporting is a dated snapshot, not a second transaction system.</p></section></div>`;}
 function leaks(){let view=0,cell='',selected=0,seq=3;const reports=[{id:'LEAK-DEMO-01',cell:'B2',area:'Utility room',type:'Water',description:'Small drip at fictional overhead fitting.',hazard:true,state:'Open',note:'',photo:true},{id:'LEAK-DEMO-02',cell:'C3',area:'Stores',type:'Roof',description:'Damp patch after synthetic rain event.',hazard:false,state:'Assigned',note:'Assigned to Demo Facilities Coordinator.',photo:false}];
 const plan=()=>`<div class="ops-plan"><svg viewBox="0 0 600 360" role="img" aria-label="Entirely fictional L-shaped campus with receiving, assembly, stores, and utility rooms; no real plant geometry"><path class="wall" d="M30 30H570V215H415V330H30Z M30 120H210V330 M210 30V120H415V215 M415 30V215 M210 215H415"/><path class="detail" d="M52 52H115V90H52ZM133 52H189V90H133ZM242 57H294V92H242ZM319 57H372V92H319ZM245 145H287V186H245ZM318 145H362V186H318ZM450 60H533V92H450ZM450 127H533V164H450ZM50 170H84V265H50ZM110 170H145V265H110ZM251 247H295V292H251ZM328 247H372V292H328Z"/><path class="path" d="M170 145V300M220 105H395M395 105V310"/><text x="45" y="111">RECEIVING</text><text x="232" y="115">ASSEMBLY</text><text x="438" y="194">UTILITIES</text><text x="58" y="308">STORES</text><text x="244" y="318">SUPPORT</text>${cell?`<circle class="marker" cx="${80+('ABCD'.indexOf(cell[0]))*140}" cy="${65+(+cell[1]-1)*100}" r="12"/>`:''}</svg><p class="ops-caption">Fictional plan · schematic only · not to scale</p></div>`;
 function render(){root.dataset.view=view;root.innerHTML=heading('Facility Leak Management','Employee reporting / Facilities response')+tabs(['Report a leak','Facilities queue'],view)+`<div class="ops-pad">${view===0?`<div class="ops-columns"><section class="ops-card"><h2>Report a Facility Leak</h2><p class="ops-caption">1. Select the location on the synthetic campus.</p>${plan()}<div class="ops-grid" aria-label="Map grid location">${['A1','B1','C1','D1','A2','B2','C2','D2','A3','B3','C3'].map(c=>`<button type="button" data-cell="${c}" aria-pressed="${cell===c}">${c}</button>`).join('')}</div><p id="cell-status">${cell?'Selected grid: '+cell:'Choose a grid cell before continuing.'}</p>${action('Continue','continue',true)}</section><section class="ops-card" id="leak-fields" hidden><h2>Leak Details</h2><form id="leak-form"><label>Area *<input name="area" required maxlength="60" placeholder="Fictional area"></label>${select('Leak type *','leak-type',['Water','Roof','Air','Other'])}<label>Description *<textarea name="description" required minlength="10" maxlength="400" rows="3"></textarea></label><label><input type="checkbox" name="hazard">Potential safety hazard</label><p class="ops-notice">An urgent hazard requires the site’s immediate safety response. A form does not replace emergency reporting.</p><label><input type="checkbox" name="photo">Attach synthetic photo placeholder (no upload)</label><button class="primary" type="submit">Submit report</button></form></section></div>`:`${kpis([['Open',reports.filter(r=>r.state!=='Closed').length],['Hazards',reports.filter(r=>r.hazard&&r.state!=='Closed').length],['Closed',reports.filter(r=>r.state==='Closed').length]])}<div class="ops-columns"><section><h2>Open Facility Leaks</h2><p class="ops-caption">Green marker: selected report · Hazard labels identify priority review.</p><div class="ops-queue">${reports.map((r,i)=>`<button type="button" data-report="${i}" aria-pressed="${i===selected}"><strong>${r.id} · ${esc(r.area)}</strong><span class="ops-badge ${r.hazard?'ops-hazard':''}">${r.hazard?'Hazard · ':''}${r.state}</span><small>Grid ${r.cell} · ${r.type} · Demo Reporter · Sep 14, 2026</small></button>`).join('')}</div></section><section class="ops-card"><h2>${reports[selected].id}</h2>${plan()}<p>${esc(reports[selected].description)}</p><p>Attachment: ${reports[selected].photo?'Synthetic photo placeholder':'None'}</p><p>State: <strong>${reports[selected].state}</strong></p><p>${esc(reports[selected].note)}</p><label>Response / verification note<textarea id="response-note" maxlength="400"></textarea></label><div class="ops-actions">${reports[selected].state==='Open'?action('Assign to facilities','assign',true):reports[selected].state==='Assigned'?action('Record repair complete','repair',true):reports[selected].state==='Awaiting verification'?action('Verify and close','close',true):'<p class="ops-notice ops-success">Verified closure recorded. Original report retained.</p>'}</div></section></div>`}</div>`;
 wireTabs(i=>{view=i;cell=view?reports[selected].cell:cell;render();focusTitle();});
 root.querySelectorAll('[data-cell]').forEach(b=>b.onclick=()=>{cell=b.dataset.cell;root.querySelectorAll('[data-cell]').forEach(x=>x.setAttribute('aria-pressed',String(x.dataset.cell===cell)));root.querySelector('#cell-status').textContent='Selected grid: '+cell;const marker=root.querySelector('.ops-plan .marker');if(marker)marker.remove();const svg=root.querySelector('.ops-plan svg');const pin=document.createElementNS('http://www.w3.org/2000/svg','circle');pin.setAttribute('class','marker');pin.setAttribute('cx',80+'ABCD'.indexOf(cell[0])*140);pin.setAttribute('cy',65+(+cell[1]-1)*100);pin.setAttribute('r',12);svg.appendChild(pin);});
 root.querySelectorAll('[data-report]').forEach(b=>b.onclick=()=>{selected=+b.dataset.report;cell=reports[selected].cell;render();});
 const cont=root.querySelector('#continue');if(cont)cont.onclick=()=>{if(!cell){message('Select a map grid first.');return;}root.querySelector('#leak-fields').hidden=false;root.querySelector('[name=area]').focus();};
 const form=root.querySelector('#leak-form');if(form)form.onsubmit=e=>{e.preventDefault();const f=new FormData(form);if(!String(f.get('area')).trim()||String(f.get('description')).trim().length<10){message('Enter at least 10 non-space characters.');return;}reports.unshift({id:'LEAK-DEMO-'+String(seq++).padStart(2,'0'),cell,area:String(f.get('area')).trim(),type:root.querySelector('#leak-type').value,description:String(f.get('description')).trim(),hazard:f.has('hazard'),photo:f.has('photo'),state:'Open',note:'Submitted by Demo Reporter. Facilities response required.'});selected=0;view=1;render();message('Report created. The queue now contains your synthetic report.');focusTitle();};
 ['assign','repair','close'].forEach(id=>{const b=root.querySelector('#'+id);if(b)b.onclick=()=>{const note=root.querySelector('#response-note').value.trim();if(note.length<10){message('Enter a meaningful note of at least 10 characters before changing state.');root.querySelector('#response-note').focus();return;}const r=reports[selected];r.state={assign:'Assigned',repair:'Awaiting verification',close:'Closed'}[id];r.note+=(id==='assign'?' Facilities Coordinator: ':' Verification history: ')+note;render();message('Recorded: '+r.state+'.');};});
 }render();}
 function service(){root.innerHTML=heading('Service issue report','Simple Forms-style intake · synthetic data only')+`<form class="ops-form" id="service-form"><h2>Service / Janitorial Issue Report</h2><p>Report a condition so Facilities can review the concern and coordinate follow-up.</p><p>Use fictional details. Required fields are marked *.</p><fieldset><legend>1. Where did you observe the issue? *</legend>${['Receiving','Assembly','Stores','Break room'].map(v=>`<label><input type="radio" name="service-area" value="${v}" required>${v}</label>`).join('')}</fieldset><fieldset><legend>2. What needs attention? *</legend>${['Cleaning','Restroom supplies','Waste collection','Other'].map(v=>`<label><input type="radio" name="service-type" value="${v}" required>${v}</label>`).join('')}</fieldset><label>Description *<textarea required minlength="10" maxlength="400" name="description" rows="4"></textarea></label><label><input type="checkbox" name="photo">Include a synthetic attachment placeholder</label><p class="ops-notice">Urgent safety issue? Use the established emergency or supervisor channel immediately. This demonstration does not contact anyone.</p><button class="primary" type="submit">Submit request</button></form>`;root.querySelector('form').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target);if(String(f.get('description')).trim().length<10){message('Enter a meaningful description of at least 10 characters.');return;}e.target.innerHTML=`<h2>Request received</h2><p class="ops-notice ops-success">SR-DEMO-001 · Routed to Demo Facilities Coordinator</p><p>${esc(f.get('description'))}</p><p>Attachment: ${f.has('photo')?'Synthetic placeholder':'None'}</p>${action('Submit another fictional request','another')}`;root.querySelector('#another').onclick=service;message('Synthetic request recorded in this page only.');focusTitle();};}
 function talks(){let view=location.hash==='#admin'?1:0,topic=0,opened=false,complete=false,attempts=0,snapshot=false,dept='All';const topics=['Powered industrial trucks & pedestrians','Crane and hoist awareness'];const topicStates={};function switchTopic(next){topicStates[topic]={opened,complete,attempts,snapshot};topic=next;const saved=topicStates[topic]||{};opened=!!saved.opened;complete=!!saved.complete;attempts=saved.attempts||0;snapshot=saved.snapshot||null;render();}
 function render(){root.dataset.platform=view===1?'report':'sharepoint';root.innerHTML=heading('Toolbox Talks / Charlas de seguridad','Manufacturing Campus · Learning & acknowledgment')+tabs(['Materials & quiz','Completion administration'],view)+`<div class="ops-pad">${view===0?`<section class="ops-band"><h2>Read. Discuss. Confirm.</h2><p>Open the material for the selected month, then complete the acknowledgment. This illustrative content is not workplace safety training.</p></section><h2>2026 · September</h2>${select('Topic','talk-topic',topics)}<div class="ops-columns"><section class="ops-card"><h3>${topics[topic]}</h3>${action('Open material / Abrir material','open-material',true)}<div id="material" ${opened?'':'hidden'}><h3>Demonstration briefing</h3><p>Stop and ask the designated supervisor when a route or operating condition is unclear. Follow approved site procedures and equipment-specific training.</p><p>Ante una condición poco clara, deténgase y consulte al supervisor. Siga los procedimientos autorizados.</p><p class="ops-caption">Synthetic learning excerpt. Not a certification or authorization to operate equipment.</p></div></section><section class="ops-card"><h3>Quiz & acknowledgment</h3><form id="talk-form"><fieldset><legend>What should you do when a condition is unclear?</legend><label><input type="radio" name="answer" value="ask" required>Stop and consult the designated supervisor</label><label><input type="radio" name="answer" value="continue">Continue without checking</label></fieldset><label><input type="checkbox" name="ack" required>I reviewed the demonstration material</label><button type="submit" class="primary" ${!opened||complete?'disabled':''}>${complete?'Completion recorded':'Submit acknowledgment'}</button></form>${complete?'<p class="ops-notice ops-success">Demo Learner 01 · September · Completion recorded in operational records.</p>':''}</section></div>`:`<div class="ops-report-title"><strong>LEARNING COMPLETION</strong><small>Scheduled report snapshot · ${snapshot?'Refreshed in this session':'Seeded snapshot'}</small></div><div class="ops-filters">${select('Year','talk-year',['2026'])}${select('Month','talk-month',['September'])}${select('Topic','admin-topic',topics)}${select('Department','talk-dept',['All','Operations','Facilities'])}</div>${kpis([['Completed',dept==='Facilities'?1:((snapshot&&snapshot.complete?4:3)-(dept==='Operations'?1:0))],['Required',dept==='All'?6:dept==='Operations'?4:2],['Completion',dept==='Facilities'?'50%':number(((snapshot&&snapshot.complete?4:3)-(dept==='Operations'?1:0))/(dept==='All'?6:4)*100,1)+'%'],['Records','Synthetic']])}<div class="ops-columns">${bars('Department completion',[['Operations',(snapshot&&snapshot.complete?3:2)/4*100],['Facilities',50]])}<article class="ops-card"><h3>Record vs report</h3><p>A submitted acknowledgment is recorded immediately in the simulated operational list. The report only changes after refreshing the snapshot.</p>${action('Refresh report snapshot','refresh-talk',true)}</article></div>${table('Employee-module detail',['Learner','Department','Topic','Status','Attempts','Latest'],[['Demo Learner 01','Operations',topics[topic],snapshot&&snapshot.complete?'Completed':'Required',snapshot?snapshot.attempts:0,snapshot&&snapshot.complete?'Current session':'—'],['Demo Learner 02','Operations',topics[topic],'Completed',1,'Sep 12'],['Demo Learner 03','Facilities',topics[topic],'Completed',1,'Sep 13'],['Demo Learner 04','Operations',topics[topic],'Completed',1,'Sep 11'],['Demo Learner 05','Operations',topics[topic],'Required',0,'—'],['Demo Learner 06','Facilities',topics[topic],'Required',0,'—']].filter(r=>dept==='All'||r[1]===dept))}`}</div>`;
 wireTabs(i=>{view=i;render();});
 const t=root.querySelector('#talk-topic');if(t){t.value=topics[topic];t.onchange=()=>{switchTopic(topics.indexOf(t.value));};}
 const at=root.querySelector('#admin-topic');if(at){at.value=topics[topic];at.onchange=()=>{switchTopic(topics.indexOf(at.value));};}
 const open=root.querySelector('#open-material');if(open)open.onclick=()=>{opened=true;render();message('Material opened. The acknowledgment is now available.');};
 const form=root.querySelector('#talk-form');if(form)form.onsubmit=e=>{e.preventDefault();attempts++;if(new FormData(form).get('answer')!=='ask'){message('Review the material and try again. No completion has been recorded.');return;}complete=true;render();message('Acknowledgment recorded. Refresh the administration snapshot to see the new completion.');};
 const refresh=root.querySelector('#refresh-talk');if(refresh)refresh.onclick=()=>{snapshot={complete,attempts};render();message('Report snapshot refreshed from the simulated completion records.');};
 const d=root.querySelector('#talk-dept');if(d){d.value=dept;d.onchange=()=>{dept=d.value;render();};}
 }render();}
 function electrical(){root.dataset.platform='report';let page=0,filters={start:'2026-09-01',end:'2026-09-28',meter:'Campus',type:'All',temp:'All',dow:'All'};
 const labels=['Energy Overview','Demand & Load Patterns','Performance Evidence','Weather & Seasonal Context','Delivery & Billing'];
 function render(){const factor=filters.meter==='Support'?0.28:1;const records=data.intervals.filter(r=>r.date>=filters.start&&r.date<=filters.end&&(filters.type==='All'||(filters.type==='Weekend')===r.weekend)&&(filters.temp==='All'||(filters.temp==='Warm')===(r.temperature>=70))&&(filters.dow==='All'||+filters.dow===r.dow)).map(r=>({...r,kw:r.kw*factor,kwh:r.kwh*factor}));
 const sum=records.reduce((a,r)=>a+r.kwh,0),peak=Math.max(0,...records.map(r=>r.kw)),avg=records.length?sum*2/records.length:0,off=records.filter(r=>r.slot<12||r.slot>=36).reduce((a,r)=>a+r.kwh,0),days=[...new Set(records.map(r=>r.date))];
 const daily=days.map(date=>{const r=records.filter(i=>i.date===date);return {date,kwh:r.reduce((s,v)=>s+v.kwh,0),peak:Math.max(...r.map(v=>v.kw)),min:Math.min(...r.map(v=>v.kw)),temp:r[0].temperature};});
 const profile=Array.from({length:48},(_,slot)=>{const r=records.filter(v=>v.slot===slot);return r.length?r.reduce((a,v)=>a+v.kw,0)/r.length:0;});
 root.innerHTML=`<div class="ops-report-title"><strong>MANUFACTURING CAMPUS | ELECTRICAL USAGE</strong><small>Fictional fixture · 28 complete days · 30-minute intervals</small></div><div class="evidence-key"><span class="measured"><strong>MEASURED structure</strong> · synthetic intervals</span><span>ESTIMATED · CO₂</span><span class="modeled">MODELED · delivery cost</span><span>MISSING · prior year / supply</span><span>PARTIAL · selected window, not full bill</span></div>`+tabs(labels,page)+`<div class="ops-filters"><label>From<input type="date" id="energy-start" min="2026-09-01" max="2026-09-28" value="${filters.start}"></label><label>Through<input type="date" id="energy-end" min="2026-09-01" max="2026-09-28" value="${filters.end}"></label>${select('Meter','energy-meter',['Campus','Support'])}${select('Day type','energy-type',['All','Weekday','Weekend'])}${select('Temperature band','energy-temp',[['All','All'],['Warm ≥70°F','Warm'],['Cool <70°F','Cool']])}${select('Day of week','energy-dow',[['All','All'],...['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map((d,i)=>[d,i])])}</div><div class="ops-pad"><h2>${labels[page]}</h2>${!records.length?'<p class="ops-notice">No intervals match these filters. Adjust the dates or slicers; zero is not an observed result.</p>':page===0?`${kpis([['Synthetic energy',number(sum)+' kWh'],['Maximum 30-minute demand',number(peak,1)+' kW'],['Estimated CO₂',number(sum*.35/1000,2)+' t'],['Outside 06:00–18:00',number(off/sum*100,1)+'%']])}${line('Daily electricity use',daily.map(d=>d.kwh),'kWh')}${bars('Average interval demand',[['06–18',records.filter(r=>r.slot>=12&&r.slot<36).reduce((a,r)=>a+r.kw,0)/(records.length/2)],['18–06',off*2/(records.length/2)]],peak,' kW')}<p class="ops-caption">Energy = sum of interval kW × 0.5 hours. CO₂ uses an illustrative 0.35 kg/kWh assumption, not a verified emissions factor.</p>`:page===1?`${kpis([['Mean interval load',number(avg,1)+' kW'],['95th percentile',number(records.map(r=>r.kw).sort((a,b)=>a-b)[Math.floor((records.length-1)*.95)],1)+' kW'],['Minimum interval',number(Math.min(...records.map(r=>r.kw)),1)+' kW'],['Peak / average',number(peak/avg,2)+'×']])}${line('Typical half-hour profile',profile,'kW')}${table('Half-hour demand matrix',['Interval',...['Sun','Mon','Tue','Wed','Thu','Fri','Sat']],Array.from({length:48},(_,s)=>[`${String(Math.floor(s/2)).padStart(2,'0')}:${s%2?'30':'00'}`,...Array.from({length:7},(_,d)=>{const v=records.filter(r=>r.slot===s&&r.dow===d);return v.length?number(v.reduce((a,r)=>a+r.kw,0)/v.length,1):'—';})]))}`:page===2?`${kpis([['Selected complete days',days.length],['Interval completeness','100%'],['Prior-year coverage','Unavailable'],['Matched savings claim','Not supported']])}<p class="ops-notice">There is no prior-year comparison in this fixture. Observed differences are investigation cues—not verified savings. Production changes and coverage must be reconciled before attributing improvement.</p>${line('Daily minimum demand',daily.map(d=>d.min),'kW')}`:page===3?`${kpis([['Mean daily temperature',number(daily.reduce((a,d)=>a+d.temp,0)/days.length,1)+' °F'],['Weather coverage','Complete fixture'],['Maximum demand',number(peak,1)+' kW']])}${bars('Mean demand by temperature band',[['Below 70°F',records.filter(r=>r.temperature<70).reduce((a,r)=>a+r.kw,0)/Math.max(1,records.filter(r=>r.temperature<70).length)],['70°F or more',records.filter(r=>r.temperature>=70).reduce((a,r)=>a+r.kw,0)/Math.max(1,records.filter(r=>r.temperature>=70).length)]],peak,' kW')}<p class="ops-notice">Temperature provides context, not causation. Different operating schedules and production loads also affect demand.</p>`:`${kpis([['Selected synthetic usage',number(sum)+' kWh'],['Modeled delivery at $0.035/kWh','$'+number(sum*.035,2)],['Supply evidence','Not provided'],['All-in cost','Unavailable']])}<p class="ops-notice">Delivery-only cost must not be described as the total electricity bill. The fixture excludes supply, demand charges, taxes, and adjustments. Date slicers show a modeled subset, not a utility invoice.</p>${table('Synthetic billing boundary',['Component','Basis','Coverage'],[['Delivery',esc('$0.035 × selected kWh'),'Modeled'],['Supply','No invoice fixture','Missing'],['Demand / tax / adjustments','Excluded','Missing'],['Full bill','Cannot calculate','Not supported']])}`}${records.length?table('Daily interval evidence',['Date','kWh','Peak kW','Min kW','Temperature °F'],daily.map(d=>[d.date,number(d.kwh,1),number(d.peak,1),number(d.min,1),d.temp])):''}<p class="ops-caption">All charts and values derive from the same deterministic synthetic intervals. Support meter is an illustrative 28% allocation of campus load, not a separately metered source.</p></div>`;
 wireTabs(i=>{page=i;render();});Object.keys(filters).forEach(key=>{const el=root.querySelector('#energy-'+key);el.value=filters[key];el.onchange=()=>{filters[key]=el.value;render();message('Report filters applied.');};});
 }render();}
 function fleet(){root.dataset.platform='report';let page=0,truck='All',period='Full shift',guard=30;const labels=['Shipping Operations','Facilities & Energy','Materials / Fleet Readiness','Optimization / Charging Timing Pilot'];
 function render(){const rows=data.trucks.filter(t=>truck==='All'||t.id===truck),n=rows.length,modelEnergy=rows.reduce((a,t)=>a+(100-t.soc)*.36,0)*(period==='Full shift'?1:.5),site=data.intervals.filter(r=>r.date==='2026-09-14').map(r=>r.kw),chargeKw=modelEnergy/3,charge=site.map((_,i)=>i>=14&&i<=19?chargeKw:0),minSoc=Math.min(...rows.map(t=>t.soc))-12;
 root.innerHTML=`<div class="ops-report-title"><strong>MANUFACTURING CAMPUS | FORKLIFT FLEET & BATTERY</strong><small>Fictional snapshot · Sep 14, 2026, 14:00 · not live telemetry</small></div><div class="evidence-key"><span class="measured">MEASURED structure · synthetic snapshot</span><span class="modeled">MODELED · charging</span><span>UPPER BOUND · not achieved savings</span></div>`+tabs(labels,page)+`<div class="ops-filters">${select('Truck','fleet-truck',['All',...data.trucks.map(t=>t.id)])}${select('Evidence window','fleet-period',['Full shift','Half shift'])}</div><div class="ops-pad"><h2>${labels[page]}</h2>${page===0?`${kpis([['Working now',rows.filter(t=>t.state==='Working').length],['Idle now',rows.filter(t=>t.state==='Idle').length],['Charging now',rows.filter(t=>t.state==='Charging').length],['Usable snapshot',rows.filter(t=>t.age<=15&&t.errors===0&&t.soc>=30&&t.state!=='Charging').length+' / '+n]])}<p class="ops-notice">Readiness requires charge, freshness, and no battery errors. “Idle” alone does not prove that a truck is available.</p><div class="ops-columns">${bars('Selected-period utilization',rows.map(t=>[t.id,t.use]))}${bars('Snapshot state of charge',rows.map(t=>[t.id,t.soc]))}</div>${line('Synthetic hourly active-truck count',Array.from({length:24},(_,h)=>h>=6&&h<18?Math.min(n,1+(h%3)):0),'trucks')}`:page===1?`${kpis([['Modeled charging energy',number(modelEnergy,1)+' kWh'],['Direct cost at $0.10/kWh','$'+number(modelEnergy*.1,2)],['Modeled coincident charging',number(chargeKw,1)+' kW'],['Annual upper bound','$'+number(chargeKw*.6*12*8)]])}${line('Site load and modeled charging',site,'kW · one synthetic day',charge)}<p class="ops-notice">Blue is a synthetic meter series; amber is a charging model. The annual upper bound assumes 60% of modeled charging shifts away from the peak, $8/kW-month, and full peak coincidence for 12 months. It is not an achieved saving and excludes operational constraints.</p>`:page===2?`${kpis([['Stale snapshots',rows.filter(t=>t.age>15).length],['BMS exceptions',rows.filter(t=>t.errors>0).length],['Snapshot rule','≤15 min'],['Charge threshold','30%']])}<p class="ops-notice">Stale data and battery errors require human review. No battery or machine commands are available.</p>`:`${kpis([['Synthetic evidence days','1'],['Modeled energy shifted',number(modelEnergy*.6,1)+' kWh'],['Peak reduction upper bound',number(chargeKw*.6,1)+' kW'],['Lowest modeled SOC',number(minSoc)+'%']])}<label>Minimum acceptable SOC <input id="soc-guard" type="number" min="10" max="90" value="${guard}"></label>${action('Evaluate charge guardrail','evaluate-guard',true)}<p class="ops-notice ${minSoc>=guard?'ops-success':''}">${minSoc>=guard?'Charge threshold passes this limited model.':'Guardrail fails: do not shift charging under this assumption.'} This is decision support only. Shipping, battery vendor, and safety owners must approve a controlled pilot. One day is insufficient to establish repeatability.</p>${bars('Modeled SOC after 12-point shift',rows.map(t=>[t.id,Math.max(0,t.soc-12)]))}<p>Missing evidence: matched-shift readiness, actual later charging, charger efficiency, repeatable demand coincidence, and service continuity.</p>`}${table('Fleet snapshot',['Truck','Battery','State','SOC %','Age min','BMS errors','Use %'],rows.map(t=>[t.id,t.battery,t.state,t.soc,t.age+(t.age>15?' · STALE':''),t.errors,t.use]))}<p class="ops-caption">All records synthetic. Charging assumes a 36 kWh battery, refill from displayed SOC to 100%, no charging losses, and a three-hour constant-power window. Half-shift coverage scales modeled energy by 50%; it does not alter the snapshot. Window selection changes modeled energy coverage; the latest-state snapshot and utilization fixture remain separately labeled. No achieved savings are claimed.</p></div>`;
 wireTabs(i=>{page=i;render();});const t=root.querySelector('#fleet-truck');t.value=truck;t.onchange=()=>{truck=t.value;render();};const p=root.querySelector('#fleet-period');p.value=period;p.onchange=()=>{period=p.value;render();};const g=root.querySelector('#soc-guard');if(g)root.querySelector('#evaluate-guard').onclick=()=>{guard=Math.min(90,Math.max(10,+g.value||30));render();message('Charge threshold evaluated. Review the modeled result and required approvals.');};
 }render();}
 function compliance(){let tab=location.hash==='#report'?3:0,selected=0,snapshot=null;const tasks=[{id:'TASK-DEMO-201',name:'Review supplies station labels',due:12,owner:'Facilities Coordinator',status:'Not started',evidence:false,note:'',action:false},{id:'TASK-DEMO-202',name:'Check demonstration record index',due:18,owner:'Records Coordinator',status:'In progress',evidence:false,note:'',action:false},{id:'TASK-DEMO-203',name:'Review synthetic document links',due:25,owner:'Facilities Coordinator',status:'Complete',evidence:true,note:'Sample links reviewed.',action:false}];const copy=()=>tasks.map(t=>({...t}));snapshot=copy();
 function render(){root.dataset.platform=tab===3?'report':'sharepoint';const t=tasks[selected];root.innerHTML=heading('EHS Compliance Control','Synthetic internal requirements · not regulatory guidance')+tabs(['Calendar','Requirement guide','Task execution','Management snapshot'],tab)+`<div class="ops-pad">${tab===0?`<h2>September 2026 · task calendar</h2><p>Select a due task. Reporting date is fixed at September 14 for this fixture.</p><div class="ops-calendar">${Array.from({length:30},(_,i)=>`<div class="ops-day"><strong>Sep ${i+1}</strong>${tasks.map((r,j)=>r.due===i+1?`<button type="button" data-task="${j}">${r.name}<br>${r.status}${r.due<14&&r.status!=='Complete'?' · Overdue':''}</button>`:'').join('')}</div>`).join('')}</div>`:tab===1?`<h2>Requirement guide · DEMO-REQ-01</h2><div class="ops-card"><h3>Plain-English Explanation</h3><p><strong>Supplies station label review</strong></p><p>Internal demonstration standard: keep fictional stock-point labels readable and consistent with the item register.</p><div class="ops-notice"><strong>Why This Exists</strong><p>A clear signal helps the correct item reach the correct stock point.</p></div><h3>Required Plant Action</h3><p>Compare label to the synthetic item register; record condition and discrepancies.</p>${table('Ownership and Execution',['Role','Responsibility'],[['Facilities Coordinator','Assigns and follows up'],['Demo Inspector','Executes and records evidence'],['Records Coordinator','Confirms record completeness']])}<div class="ops-notice ops-success"><strong>Completion and Evidence</strong><p>All listed stations reviewed, findings recorded, and an evidence reference attached to the scheduled occurrence.</p></div><div class="ops-notice"><strong>What Does Not Count</strong><p>A verbal “done,” a calendar reminder, or an attachment without an identified task is not completion evidence.</p></div><div class="ops-notice ops-hazard"><strong>Action Log Trigger</strong><p>Missing or conflicting labels create an owned follow-up action; do not silently mark the task complete.</p></div><h3>Official Reference</h3><p> fictional internal standard DEMO-STD-01. No external regulatory interpretation is reproduced.</p>${action('Open execution record','open-execution',true)}</div>`:tab===2?`<h2>Task execution</h2>${select('Scheduled occurrence','task-select',tasks.map((r,i)=>[r.id+' · '+r.name,i]))}<form class="ops-card" id="task-form"><p><strong>${t.name}</strong> · Due Sep ${t.due}</p>${select('Owner','task-owner',['Facilities Coordinator','Records Coordinator'])}${select('Status','task-status',['Not started','In progress','Complete','Exception'])}<label>Execution / exception note<textarea id="task-note" maxlength="400">${esc(t.note)}</textarea></label><label><input type="checkbox" id="task-evidence" ${t.evidence?'checked':''}>Attach synthetic evidence reference</label><p class="ops-caption">Completion requires evidence. Exceptions require a meaningful note and create an action-log entry.</p><button type="submit" class="primary">Validate and save</button>${t.action?'<p class="ops-notice">ACTION-DEMO-01 · Assigned follow-up retained.</p>':''}</form><p><a href="../compliance-workflow/">Inspect the existing master → occurrence architecture demo →</a></p>`:`<div class="ops-report-title"><strong>REQUIREMENT MANAGEMENT</strong><small>2026 · Scheduled snapshot, not the live task list</small></div>${kpis([['Active fixture requirements',3],['Complete',snapshot.filter(r=>r.status==='Complete').length],['Overdue',snapshot.filter(r=>r.due<14&&r.status!=='Complete').length],['Completion rate',number(snapshot.filter(r=>r.status==='Complete').length/snapshot.length*100,1)+'%'],['Remaining',snapshot.filter(r=>r.status!=='Complete').length],['Due next 7 days',snapshot.filter(r=>r.due>=14&&r.due<=21&&r.status!=='Complete').length],['Evidence needed',snapshot.filter(r=>!r.evidence).length]])}${action('Refresh management snapshot','refresh-compliance',true)}<p class="ops-caption">Snapshot only updates when refreshed. The implemented environment uses scheduled Power BI refresh; operational Lists records are the source of truth.</p>${bars('Tasks by status',['Not started','In progress','Complete','Exception'].map(s=>[s,snapshot.filter(r=>r.status===s).length]),3,'')}${table('Task detail',['Task','Due','Owner','Category','Status','Evidence'],snapshot.map(r=>[r.id,'Sep '+r.due,r.owner,'Internal standard',r.status,r.evidence?'Attached':'Needed']))}`}</div>`;
 wireTabs(i=>{tab=i;render();});root.querySelectorAll('[data-task]').forEach(b=>b.onclick=()=>{selected=+b.dataset.task;tab=2;render();focusTitle();});const open=root.querySelector('#open-execution');if(open)open.onclick=()=>{tab=2;render();};const f=root.querySelector('#task-form');if(f){root.querySelector('#task-select').value=selected;root.querySelector('#task-select').onchange=e=>{selected=+e.target.value;render();};root.querySelector('#task-owner').value=t.owner;root.querySelector('#task-status').value=t.status;f.onsubmit=e=>{e.preventDefault();const status=root.querySelector('#task-status').value,evidence=root.querySelector('#task-evidence').checked,note=root.querySelector('#task-note').value.trim();if(status==='Complete'&&!evidence){message('Completion blocked: attach an evidence reference.');return;}if(status==='Exception'&&note.length<10){message('Exception blocked: enter at least 10 characters explaining the issue.');return;}Object.assign(t,{status,evidence,note,owner:root.querySelector('#task-owner').value,action:t.action||status==='Exception'});render();message('Operational task saved. Management snapshot has not refreshed yet.');};}const refresh=root.querySelector('#refresh-compliance');if(refresh)refresh.onclick=()=>{snapshot=copy();render();message('Management snapshot refreshed.');};
 }render();}
 ({'plant-operations-hub':hub,'facility-leaks':leaks,'service-intake':service,'toolbox-talks':talks,'electrical-analytics':electrical,'forklift-fleet':fleet,'ehs-control':compliance}[kind]||hub)();
})();
